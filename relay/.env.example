# Copy to .env and fill in. Never commit .env. The relay reads these
# from the environment only -- nothing here is ever written into
# state/shared_state.json or history/log.jsonl.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
